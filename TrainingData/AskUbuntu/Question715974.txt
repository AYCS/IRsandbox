[{"id":715974,"title":"When counting lines with wc, don&#39;t print an error whenever is a directory","body":"<p>I want to count the total number of lines in all <code>/etc</code> files but not the files in the sub directories, so I typed: <code>wc -l /etc/* | tail -1</code> and the output is like:</p>\n\n<pre><code>xxxx is a directory \nyyyy is a directory\ntotal 1752\n</code></pre>\n\n<p>My question is, how can I rid of (delete) these comments, and is there a better way to do this type of count?</p>\n","related_questions":[{"id":703984,"title":"counting in directory","body":"<p>I would like to know what the difference is between:</p>\n\n<pre><code>$ ls | wc -l\n</code></pre>\n\n<p>and</p>\n\n<pre><code>$ ls &gt; foo\n$ wc -l &lt; foo\n</code></pre>\n\n<p>when counting all files in the current directorys. And why does the second one gives me one file more.</p>\n"},{"id":686693,"title":"counting specific file in directory","body":"<p>Let assume, I need to count every file in directory which ends with o. (for example ab12.14o, 70010340.09o). Which command I need to use?</p>\n\n<p>I tried <code>ls *.o | wc</code> but it doesn't work.</p>\n"}]}]
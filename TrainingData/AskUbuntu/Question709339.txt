[{"id":709339,"title":"Byobu creates new icon in unity launcher","body":"<p>I have locked the byobu icon in my unity menu. When I click it I would expect to have a window for this icon. </p>\n\n<p>But this does not happen. What actually happens is when I click it for the first time, it opens a Terminal window, with it's own, new icon in the menu (so now I have an byobu icon which looks like is not started, and an unwanted terminal icon). If I click the byobu icon a second time, it opens a new terminal window.</p>\n\n<p>I have this problem since I got a new laptop (ubuntu 15.10). On my old laptop it works as expected (ubuntu 14.04 LTS).</p>\n\n<p>How can I fix this?</p>\n\n<p>Edit:\nFrom the comments:</p>\n\n<p>The Exec line in /usr/share/applications/byobu.desktop:</p>\n\n<pre><code>Exec=env TERM=xterm-256color byobu\n</code></pre>\n","related_questions":[{"id":13758,"title":"How can I edit/create new launcher items in Unity by hand?","body":"<p>Will Unity allow making custom launcher icons from <code>.desktop</code> files or via menu editing system? (Right now the launcher doesn't give the option to \"keep in launcher\" on all programs.</p>\n\n<p>For some programs I use, I have to make custom launchers or <code>.desktop</code> files.</p>\n\n<p>For instance, daily blender builds are generally just folders with an executable.</p>\n\n<p>In basic Gnome or KDE, I can make a new menu entry with the menu editing system. Then, I can also add it to Docky either from the menu or by dragging a <code>.desktop</code> file to it. Unity launcher doesn't support drag and drop, so thats not a bug or anything, but when I open a <code>.desktop</code> file, it has unpredictable results. Most time it will not have \"keep in launcher\". Sometime it will have a pinnable item without the <code>.desktop</code>'s icon, and if I pin the item to the launcher, it will not call upon the program again after closing it. I've also gotten it to just work with a <code>.desktop</code> file for <code>celtx</code>.</p>\n"},{"id":696080,"title":"How do I get byobu to create and set up certain windows every time I start it?","body":"<p>I am using byobu v5.98 using tmux 1.9 backend on Ubuntu 15.04.</p>\n\n<p>I find myself creating the same 3 windows (plus splits), in the same way, each time I start up my laptop.  Obviously this is less than ideal.  I want to be able to start byobu and have it create the windows and splits for me.</p>\n\n<p>The Windows section of <a href=\"http://manpages.ubuntu.com/manpages/vivid/en/man1/byobu.1.html\" rel=\"nofollow\">the byobu manpage</a> tells me to add the file <code>~/.config/byobu/windows.tmux</code> in the <code>.tmuxrc</code> format, as specified in <a href=\"http://manpages.ubuntu.com/manpages/vivid/en/man1/tmux.1.html\" rel=\"nofollow\">the tmux manpage</a>.  </p>\n\n<p>I want Window 0 to have a 3-pane split, showing <code>htop</code> and two logfiles.  Window 1 should have <code>irssi</code> running.  And finally, <code>bash</code> in Window 2.</p>\n\n<p>So I have created the following and put it into my <code>~/.config/byobu/windows.tmux</code> file:</p>\n\n<pre><code>new-session 'bash' ;\nnew-window -n mon 'htop' ;\nsplit-window -h -p 50 'ctail /var/log/syslog' ;\nswap-pane ;\nsplit-window -v -p 50 -t top 'ctail /var/log/kern.log' ;\nnew-window -n irssi 'irssi' ;\nnew-window ;\n</code></pre>\n\n<p>When I run <code>byobu</code>, I get the following error message:</p>\n\n<pre><code>usage: split-window [-dhvP] [-c start-directory] [-F format] [-p percentage|-l size] [-t target-pane] [command]\n</code></pre>\n\n<p>I've checked the tmux manpage repeatedly and I can't figure out what I'm doing wrong.  </p>\n"},{"id":179368,"title":"Shift+F2 is ignored when using byobu with PuTTY (Windows 7)","body":"<p>Connecting to byobu in Ubuntu 12.04.1 (precise) from PuTTY on a Windows 7 system, when I type <kbd>Shift</kbd>+<kbd>F2</kbd> to get a vertical split, nothing happens. Typing plain <kbd>F2</kbd> works and opens a new window.</p>\n\n<p>I tried all the terminal keyboard types in PuTTY configuration (ex. XTerm R2, Linux) without any luck. Any ideas? Is there nay another keyboard shortcut to do the same thing?</p>\n"},{"id":628510,"title":"Byobu doesn&#39;t seem to launch","body":"<p>I have recently bought a VPS which is running on Ubuntu 14.04 - I have connected to it fine via SSH and have installed byobu using apt-get. I have used the byobu-enable command, but after closing the session and re-entering, the byobu interface doesn't appear - my SSH client window does have byobu in the title, but the interface for it is not present, nor are any of the keyboard commands working.</p>\n\n<p>Thanks for any help given.</p>\n"},{"id":590278,"title":"Byobu &gt; Name windows config file session","body":"<p>I'm setting up a new administration server and I would like to be able to easily bring up a set of windows depending on what I'm working on. For example a session named \"WebProd\" that would open all 4 web servers, and another \"WebTest\" that would open those two, etc.  </p>\n\n<p>In researching this I've been able to do the following from the man page:</p>\n\n<pre><code>$ cat $BYOBU_CONFIG_DIR/windows.ssh_sessions\nscreen -t localhost bash\nscreen -t aussie ssh root@aussie\nscreen -t beagle ssh root@beagle\nscreen -t collie ssh root@collie\n$ BYOBU_WINDOWS=ssh_sessions byobu\n</code></pre>\n\n<p>However, this does not name the session. I'd like to be able to name the session as part of these commands so that I don't have to rename it every time I use them and I can easily detach and re-attach.  I've tried entries such as the following to no avail:</p>\n\n<pre><code>screen -S WebProd -t Admin \nscreen -S WebProd -t Web1 ssh user@web1_server\nscreen -S WebProd -t Web2 ssh user@web2_server\n</code></pre>\n\n<p>or just naming the first line alone. I can't see it as it goes away too fast but there is an error that shows up in the 'alwayslastline' area and then disappears.</p>\n\n<p>Then later what I would like to do is setup aliases so I can just do something like</p>\n\n<pre><code>goweb1\n</code></pre>\n\n<p>and it will effectively run 'BYOBU_WINDOWS=WebProd byobu', then if I detach I can still see the session name of it and easily get back into it.  Right now they would all be the default \"byobu\" and I wouldn't know which one is WebProd and which is WebTest and so on without re-attaching to each one or manually updating the session name each time.</p>\n\n<p>I hope this isn't all clear as mud, here is a live example of what I'm looking at. I made the two .byobu/windows.webprod and .byobu/windows.webtest files and then started these sessions with my aliases. Now when I go to re-attach I see:</p>\n\n<pre><code>&gt; screen -ls\nThere are screens on:\n        12430.byobu     (Detached)\n        12349.byobu     (Detached)\n</code></pre>\n\n<p>And I don't know which is which.</p>\n"},{"id":319336,"title":"Bind byobu window to Ctrl+Shift+T shortcut","body":"<p><kbd>Ctrl</kbd>+<kbd>Shift</kbd>+<kbd>T</kbd> shortcut runs the <code>gnome-terminal</code> by default. I have installed <code>byobu</code>, and I want this shortcut to open the latest used byobu session. When pressed multiple times, I should still have just one window.</p>\n\n<p>I tried to map this shortcut to <code>gnome-terminal</code>, but this open multiple windows, with the same session.</p>\n\n<p>The behavior I want is the same as clicking on the terminal icon in the dash. After the above map I have a behavior of \"New terminal\" which can be accessed by the right click on this icon. It's probably about some gnome-terminal parameter, but I don't see any related one.</p>\n"},{"id":288730,"title":"Want to create new Dolphin window from Unity launcher","body":"<p>I installed the Dolphin File manager when Ubuntu 13.04's file manager stopped supporting expandable folders in the file pane.  Sometimes I like to have separate Dolphin instances running in my various Unity work spaces.  As it is, I can open an existing Dolphin instance, open the file menu, and select \"New Window\" to create a new Dolphin instance.  I can then move the new Dolphin instance to the appropriate workspace.</p>\n\n<p>I would rather, from appropriate workspace, right click the Dolphin file manager icon in the Unity launcher bar and select an option that says, \"New Window\".  Unfortunately, that option does not exist.</p>\n\n<p>I tried opening a terminal and entering the following commands:</p>\n\n<pre><code>gsettings get com.canonical.Unity.Launcher favorites\ncd /usr/share/applications/\ngksudo gedit unity://expo-icon\n</code></pre>\n\n<p>My effort failed; Dolphin is not launched from a *.desktop file.  Can someone please tell me how to add a \"New Window\" option to the Dolphin icon in the Unity launcher?</p>\n"},{"id":43564,"title":"how to make byobu start with terminator by default?","body":"<p>Recently, I have been using byobu and I quite like it. However, I am more of a terminator user than gnome-terminal and I want to launch byobu inside terminator instead of gnome-terminal by default.\nHow do i do that?</p>\n\n<p>edit: what i want is click the byobu icon and open byobu inside terminator instead of gnome terminal.</p>\n"},{"id":130568,"title":"create new new launcher icon in unity in 12.04","body":"<blockquote>\n  <p><strong>Possible Duplicate:</strong><br>\n  <a href=\"http://askubuntu.com/questions/13758/how-can-i-edit-create-new-launcher-items-in-unity-by-hand\">How can I edit/create new launcher items in Unity by hand?</a>  </p>\n</blockquote>\n\n\n\n<p>How to create new launcher icon in Unity?\nFor example , \ni wrote a bash script, \nand i want add to Unity panel.\nThank you.</p>\n"},{"id":95264,"title":"Quicklist gnome-terminal don&#39;t work with byobu profile","body":"<p>Yesterday I tried to create a Desktop quicklist for my gnome-terminal to open byobu with a specific byobu profile.</p>\n\n<p>There's the part of my gnome-terminal.desktop file :</p>\n\n<pre><code>[NetBook_Byobu Shortcut Group]\nName=NetBook Byobu\nExec=gnome-terminal --disable-factory --sm-client-disable --class=netbook_byobu -x BYOBU_WINDOWS=netbook-session byobu \nTargetEnvironment=Unity\n</code></pre>\n\n<p>But nothing appends when I click on the shortcut. So I tried to launch the exec command.</p>\n\n<p>gnome-terminal open with an error : </p>\n\n<pre><code>There were an error while the creation of the child process for this terminal\n\nThe execution of the Child proccess « BYOBU_WINDOWS=netbook-session » failed (no files or folder of that type)\n</code></pre>\n\n<p>But when I launch byobu with the byobu profile :</p>\n\n<pre><code>$ BYOBU_WINDOWS=netbook-session byobu\n</code></pre>\n\n<p>The command works perfectly</p>\n\n<p>I don't understand where the problem is. Maybe someone could help me ?</p>\n\n<p>Best Regard,\nWinael</p>\n"}]}]